spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    # 3306 뒤의 blog1은 쿼리에서 권한을 준 DB 이름입니다.
    url: ${jdbc:mysql://localhost:3306/My_DB?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8&allowPublicKeyRetrieval=true}
    username: ${lkj0912}
    password: ${jun100912*}


  # JPA 설정
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect


# JWT 설정
jwt:
  secret: ${mysecretkeymysecretkeymysecretkey1234567890}
  expiration: 3600000